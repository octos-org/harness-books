# 7. 操作员控制面与发布真实性

到第 6 章为止，我们回答的是“用户如何看到真相”。第 7 章回答的是另一半问题：“操作员如何足够快地理解真相，并用它做发布决策”。这一步非常关键，因为很多团队到了这里会犯一个常见错误：系统内部已经有不少事件和日志，于是误以为“可观测性差不多有了”。但对 harness 来说，真正需要的是控制面，不是日志堆。

## 7.1 操作员真正需要回答哪些问题

一个成熟的操作员视图，至少要能在几十秒内回答下面这些问题：

- 哪个任务卡住了？
- 卡在哪个阶段？
- 最近一个动作是什么？
- 哪个 validator 失败了？
- 主产物在哪里？
- 这是本任务自身问题，还是某个子 agent / 外部工具的问题？
- 该重试、回滚、继续等待，还是升级给开发者？

只看计数器是答不出这些问题的。计数器告诉你系统“最近很忙”或“失败率升高了”，但答不出“这次具体为什么错”。这就是第 3.6 节会出现操作员盲区的原因。

## 7.2 Claude Code / Codex 给了什么操作员线索

Claude Code 的 `agentSummary.ts`、`diskOutput.ts`、resume 机制说明了一个非常实用的设计取向：操作者不该被迫直接读原始 transcript 或原始日志。他需要的是摘要、最近动作、产物位置、任务归属和恢复入口。Codex 则用 `Thread` / `Turn` / `Item` 的对象拆分，提供了更适合作为 dashboard 基底的数据骨架：历史、执行和副作用被分到了不同层上，因此更容易组织成操作员叙事。[^operator-plane-ch7]

把这两者合起来，可以得到一条很稳的经验：操作员视图不该只消费 metrics，它必须消费事实流派生出来的叙事结构。

## 7.3 仪表盘应该怎样绑定到同一事实流

更稳妥的 dashboard 设计通常会固定展示以下几类信息：

- 生命周期面：当前阶段、进入阶段时间、是否违反单调性；
- 动作面：最近工具调用、最近子任务、最近摘要；
- 产物面：主产物路径、最近修改时间、是否通过验证；
- 证据面：失败 validator、证据路径、错误类别、超时信息；
- 作用域面：session / topic / worktree / owner；
- 运营面：重试次数、孤儿任务、串扰信号、待人工介入标记。

这样做的目的，不是让 dashboard 更华丽，而是让它与第 4 章那张运行时总图一一对齐。只要 dashboard 上存在“只在图表里出现、但在事实流里找不到来源”的状态，系统就又多了一条影子真相。

## 7.4 小型 fleet 不是测试补充，而是发布事实

单元测试和集成测试是必要的，但对 harness 远远不够。时序型、多表面型、跨 session 的错误，经常只有在真实主机、真实 worker、真实重连路径里才暴露出来。

因此，小型 fleet 应该被视为发布真实性的最低证明面，而不是可选 bonus：

- 至少在两台主机上跑脚本化门禁；
- 至少覆盖两个 session、一次重载、一次重连、一次子任务或外部能力调用；
- 强制输出显式诊断类别，而不是笼统的 `failed`；
- 一旦不变量被破坏，直接阻止合并或发布。

最值得门禁化的不变量通常正好来自第 3 章：

- 必需阶段缺失；
- 阶段序列非单调；
- 终态在验证前被宣布成功；
- 会话 scope 泄漏；
- 重复 research / background task；
- 子 agent 摘要和主任务事实失联。

## 7.5 从事故博物馆回流到控制面

第 3.7 节讲“失败模式博物馆”时，重点是归档。到了第 7 章，重点则变成回流：每一个高价值失败模式，最终都应该映射成 dashboard 信号、门禁脚本或 operator runbook。只有这样，事故故事才会变成控制面的免疫力，而不是写完复盘就结束。

[^operator-plane-ch7]: 本章在此处综合 Claude Code `agentSummary.ts`、`diskOutput.ts`、resume 相关实现，以及 Codex `Thread` / `Turn` / `Item` 的对象分层，用来说明操作员控制面应如何建立在统一事实流之上；对应第 19 章参考文献 21、24。

---
